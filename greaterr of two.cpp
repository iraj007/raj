#include<stdio.h>
int main(){
	float a,b;
	printf("Enter two numbers");
	scanf("%f %f",&a,&b);
		if (a>b)
		{
			printf("The greater of the a and b is %f",a);
			
		}
else {
printf("The greater of the two is %f",b);
}
}